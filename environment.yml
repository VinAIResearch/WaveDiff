name: ./envs/wavediff
channels:
  - defaults
dependencies:
  - cudatoolkit=11.3
  - pip
  - cudatoolkit-dev
  - python=3.7
  - libgcc
  - torchaudio==0.10.0
  - torchvision==0.11.0
  - pytorch==1.10.0
  - ninja
prefix: ./envs/wavediff
